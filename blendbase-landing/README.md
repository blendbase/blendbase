 blendbase.io landing page

## Getting Started

First, run the development server:

```bash
yarn install
yarn dev
```

## Deployment
The page is automatically deployed to Cloudflare Pages after every commit to the `main` branch